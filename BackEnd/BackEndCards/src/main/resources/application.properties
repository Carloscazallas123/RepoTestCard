# ===================================================================
# CONFIGURACIÓN SINCRONIZADA CON EL PANEL DE RAILWAY
# ===================================================================
# Spring Boot priorizará las variables del panel. Si estás en tu PC local,
# usará el puerto público de respaldo (19508) de forma automática.

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://acela.proxy.rlwy.net:19508/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:biGYarMyfDktKyhBhVSbLlVhTXAPivel}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# CONFIGURACIÓN DE JPA / HIBERNATE
# ===================================================================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true